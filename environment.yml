name: spider
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - matplotlib
  
  # PyTorch - CPU version (default)
  - pytorch
  - torchvision
  - torchaudio
  - cpuonly
  
  # PyTorch - GPU version (CUDA 12.1) - comment out CPU version above and uncomment below for NVIDIA GPU
  # - pytorch
  # - torchvision
  # - torchaudio
  # - pytorch-cuda=12.1
  
  # PyTorch - GPU version (CUDA 11.8) - comment out CPU version above and uncomment below for older NVIDIA GPU
  # - pytorch
  # - torchvision
  # - torchaudio
  # - pytorch-cuda=11.8
